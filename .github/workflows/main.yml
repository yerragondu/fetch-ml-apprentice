name: Nikhil Reddy Yerragondu

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
     - name: Checkout code
       uses: actions/checkout@v3

     - name: Say Hello
       run: echo "Hello DevOps from GitHub Actions!"
