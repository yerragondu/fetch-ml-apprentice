trigger:
  - nikhil  # Change this if your branch is named differently

pool:
  name: linux-agent

steps:
  - script: echo "✅ Azure DevOps pipeline running successfully!"
    displayName: "Print Hello"
